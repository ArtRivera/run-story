package com.artrivera.buildlogic.convention

/**
 * Represents all types of extensions that can be applied to a project.
 */
enum class ExtensionType {
    APPLICATION,  // Application extensions
    LIBRARY // Library extensions
}